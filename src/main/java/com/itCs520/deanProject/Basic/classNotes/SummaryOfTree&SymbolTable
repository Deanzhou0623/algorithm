一. 定义：
    符号表
        定义：键值一一对应的关系，key具有唯一性
    树
        定义：树是一对多的数据结构
        特点：
           1. 每一个结点有0个或多个子结点
           2. 没有父结点的结点为根结点
           3. 每一个非根结点只有一个父结点
           4. 每一个结点及其后代结点整体上可以看做一棵树，称为当前结点父结点的一个子树

           相关术语：
           二叉树的定义：二叉树是度不超过2度树
           满二叉树：每个结点度度都是最大值
           完全二叉树：叶子结点只能出现在在最下层，或者次下层

           二叉树的遍历：按照搜索路径的遍历 / 深度优先搜索

           前序遍历： 根左右
           中序遍历： 左 根 右
           后序遍历： 左 右 根
        堆
           定义：堆可以看作是一类特殊的数据结构，堆通常可以被看作是一颗完全二叉树对象
           特点： 完全二叉树/ 底层用数组实现 / 每个结点都大于它都子结点，子结点顺序不固定
二. 代码实现：
       符号表：
         符号表结点类Node
         符号表SymbolTable<key,value>

         有序符号表：put方法实现了Comparable接口，通过key去比较